<%= form_with(model: booking, local: true) do |form| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
        <% booking.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_mail %>
    <%= form.text_field :user_mail %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number %>
  </div>


  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number1, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number2, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number3, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number4, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number5, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number6, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number7, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number8, options_for_select(0..500)%>
  </div>

  <div class="field">
    <%= form.label :seat_number %>
    <%= form.select :seat_number9, options_for_select(0..500)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
